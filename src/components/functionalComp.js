import React from "react";
//Always remember to import any function you make.

const Greet = () => <h1>Hello</h1>;

export default Greet; //we can also import with other names

//we can also do named export
// export const Greet = () => <h1>Hello</h1>;
// but the we have to import as Greet only and we cannot change the name
